<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if (locals.activeNav && activeNav === "categories") { %>
    <title>Study Notes - Categories</title>
    <% } else if (locals.activeNav && activeNav === 'quizzes') { %>
    <title>Study Notes - Quizzes</title>
    <% } else { %>
    <title>Study Notes</title>
    <% } %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-5">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><i class="bi bi-journal-text"></i> Study Notes</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <% if (locals.activeNav && activeNav === "home") { %>
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                        <% } else { %>
                        <a class="nav-link" href="/">Home</a>
                        <% } %>
                    </li>
                    <% if (locals.loggedIn === true) { %>
                    <li class="nav-item">
                        <% if (locals.activeNav && activeNav === "categories") { %>
                        <a class="nav-link active" aria-current="page" href="/categories">Categories</a>
                        <% } else { %>
                        <a class="nav-link" href="/categories">Categories</a>
                        <% } %>
                    </li>
                    <li class="nav-item">
                        <% if (locals.activeNav && activeNav === "quizzes") { %>
                        <a class="nav-link active" aria-current="page" href="/quizzes">Quizzes</a>
                        <% } else { %>
                        <a class="nav-link" href="/quizzes">Quizzes</a>
                        <% } %>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout <small>(<%= user %>)</small></a>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <% if (locals.activeNav && activeNav === "login") { %>
                        <a class="nav-link active" aria-current="page" href="/login">Login</a>
                        <% } else { %>
                        <a class="nav-link" href="/login">Login</a>
                        <% } %>
                    </li>
                    <li class="nav-item">
                        <% if (locals.activeNav && activeNav === "register") { %>
                        <a class="nav-link active" aria-current="page" href="/register">Register</a>
                        <% } else { %>
                        <a class="nav-link" href="/register">Register</a>
                        <% } %>
                    </li>
                    <% } %>
                </ul>
                <% if (locals.loggedIn === true) { %>
                <form class="d-flex" role="search" method="post" action="/search">
                    <input class="form-control me-2" type="search" name="searchKeyword" placeholder="Search" aria-label="Search" required>
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
                <% } %>
            </div>
        </div>
    </nav>